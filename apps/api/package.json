{
  "name": "@cel/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "build": "echo \"(api) run via tsx; no bundle step\""
  },
  "dependencies": {
    "@cel/db": "workspace:*",
    "@cel/graph-client": "workspace:*",
    "@cel/report": "workspace:*",
    "@cel/rule-engine": "workspace:*",
    "@cel/types": "workspace:*",
    "@fastify/cors": "10.0.2",
    "fastify": "5.2.1",
    "zod": "3.24.1"
  },
  "devDependencies": {
    "@cel/config": "workspace:*",
    "tsx": "4.19.2"
  }
}
