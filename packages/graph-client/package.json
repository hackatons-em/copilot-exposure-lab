{
  "name": "@cel/graph-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "build": "echo \"(graph-client) source-only package — consumed as TS, no build\""
  },
  "dependencies": {
    "@cel/types": "workspace:*"
  },
  "devDependencies": {
    "@cel/config": "workspace:*",
    "@cel/rule-engine": "workspace:*"
  }
}
